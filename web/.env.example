
# Google OAuth credentials (from Google Cloud Console)
GOOGLE_CLIENT_ID={YOUR_GOOGLE_CLIENT_ID}
GOOGLE_CLIENT_SECRET={YOUR_GOOGLE_CLIENT_SECRET}
GOOGLE_CALLBACK_URL=https://{YOUR_API_DOMAIN}/auth/google/callback

# Session secret (generate a random string, at least 32 characters)
SESSION_SECRET={YOUR_SESSION_SECRET}

# Cookie domain (parent domain shared by frontend and API)
COOKIE_DOMAIN=.{YOUR_DOMAIN}

# Frontend URL (for CORS and OAuth redirect)
FRONTEND_URL=https://{YOUR_FRONTEND_DOMAIN}

# Device API key -- ESP32 devices must send this key to connect.
# Generate a random string (e.g. openssl rand -hex 32).
# This SAME value must be set in firmware/src/main.cpp WS_API_KEY.
DEVICE_API_KEY={YOUR_DEVICE_API_KEY}

# Max simultaneous device WebSocket connections (default: 100)
MAX_DEVICE_CONNECTIONS=100
